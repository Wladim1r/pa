# Crypto-Service

Приложение для отслеживания криптовалютного портфеля в реальном времени, построенное на микросервисной архитектуре с использованием Go.

## 📋 Описание

Crypto-Service — это приложение на основе микросервисов, разработанное для отслеживания криптовалютных портфелей в реальном времени. Пользователи могут регистрироваться, проходить аутентификацию, управлять своим криптопортфелем и отслеживать его стоимость через живые WebSocket-соединения.

Архитектура следует паттерну Backend-for-Frontend (BFF), где сервис Profile выступает в качестве основного шлюза для взаимодействия с пользователем. Система разделена на независимые сервисы, каждый из которых отвечает за конкретные задачи, такие как аутентификация, агрегация данных и коммуникация в реальном времени, что обеспечивает масштабируемость и разделение ответственности.

Проект также включает опциональный (в данный момент отключенный) конвейер данных для сбора и анализа исторических рыночных данных с использованием Kafka и ClickHouse.

## 🏗️ Архитектура

### Дизайн системы

Архитектура реализует паттерн распределенных микросервисов со следующими ключевыми характеристиками:

- **Паттерн BFF**: Сервис Profile выступает в роли основного пользовательского шлюза
- **Независимость сервисов**: Каждый микросервис отвечает за конкретную предметную область
- **Коммуникация в реальном времени**: WebSocket-соединения для живых обновлений портфеля
- **Событийная архитектура**: Redis Pub/Sub для асинхронного обмена сообщениями
- **Полиглот персистентность**: Множество баз данных PostgreSQL для изоляции данных

### Поток данных

```
Пользователь → Profile Service (BFF) → Authorization Service (gRPC)
                                      → Aggregator Service (HTTP/gRPC)
                                      → Socket Service (gRPC/WebSocket)
                                      → Redis Pub/Sub → ConnectionManager → WebSocket → Пользователь
```

#### 1. Поток аутентификации

- Пользователь регистрируется или входит в систему через REST API сервиса Profile
- Profile перенаправляет запрос в сервис Authorization через gRPC
- Authorization проверяет учетные данные в своей базе данных PostgreSQL
- Генерируются и возвращаются JWT-токены (access + refresh)
- Refresh-токен сохраняется в безопасных HTTP-only куки

#### 2. Управление портфелем

- Аутентифицированные пользователи управляют портфелем через API сервиса Profile
- Операции: добавление, обновление или удаление монет
- Изменения сохраняются в базе данных `postgres-profile`

#### 3. Подписка в реальном времени

- Когда пользователь добавляет монету (например, BTCUSDT), Profile уведомляет Aggregator через HTTP
- Клиентское приложение устанавливает WebSocket-соединение с сервисом Profile
- ConnectionManager регистрирует соединение и подписывается на соответствующий канал Redis Pub/Sub

#### 4. Агрегация данных

- Aggregator запрашивает поток данных у сервиса Socket через gRPC
- Socket поддерживает постоянное WebSocket-соединение с внешним API биржи
- Необработанные рыночные данные (сделки, тикеры) передаются: Биржа → Socket → Aggregator
- Aggregator обрабатывает данные, вычисляет ежесекундные обновления цен
- Обновления публикуются в каналы Redis Pub/Sub (названные по символу монеты)

#### 5. Обновления для клиента

- ConnectionManager получает обновления цен из Redis
- Пересчитывает стоимость портфеля (количество × новая цена)
- Отправляет обновленный профиль пользователя в формате JSON через WebSocket
- UI обновляется в реальном времени

## 🔧 Микросервисы

### Сервис Profile (BFF)

**Технологии**: Go, Gin, GORM, WebSockets, Redis, gRPC

**Обязанности**:
- Основной API-шлюз для взаимодействия с пользователем
- Обработка HTTP-запросов для аутентификации и управления портфелем
- Делегирование аутентификации сервису Authorization
- Управление профилями пользователей и портфелями в `postgres-profile`
- Организация потока данных путем указания Aggregator, какие потоки отслеживать
- Управление активными WebSocket-соединениями
- Доставка обновлений портфеля клиентам в реальном времени

### Сервис Authorization

**Технологии**: Go, gRPC, GORM, JWT

**Обязанности**:
- Выделенный gRPC-сервис для аутентификации и авторизации
- Управление учетными данными пользователей (имя пользователя, хешированные пароли) в изолированной базе данных `postgres-auth`
- Выпуск, проверка и обновление JSON Web Tokens
- Предоставление gRPC-методов: `Register`, `Login`, `Logout`, `Refresh`

### Сервис Aggregator

**Технологии**: Go, Redis (Pub/Sub), gRPC

**Обязанности**:
- Конвейер обработки данных, получающий необработанные данные от сервиса Socket
- Обработка различных типов рыночных данных (агрегированные сделки, мини-тикеры)
- Вычисление ежесекундных обновлений цен (SecondStat) для отслеживаемых символов
- Трансляция обновлений цен в каналы Redis Pub/Sub
- Управление подписками на потоки на основе управляющих сообщений от сервиса Profile

### Сервис Socket

**Технологии**: Go, Gorilla WebSocket, gRPC

**Обязанности**:
- Отказоустойчивый прокси для внешних WebSocket API
- Поддержка стабильных соединений с логикой автоматического переподключения
- Абстрагирование сложности внешних источников данных
- Предоставление чистых внутренних gRPC-потоков для других сервисов

## 💾 Хранение данных и обмен сообщениями

### Базы данных PostgreSQL

- **postgres-auth**: Выделенная база данных для сервиса Authorization, безопасно хранящая учетные данные пользователей
- **postgres-profile**: База данных для сервиса Profile, хранящая профили пользователей и данные портфелей

### Redis

Высокоскоростная шина сообщений в памяти, обеспечивающая коммуникацию в реальном времени между Aggregator (издатель) и Profile (подписчик) через механизм Pub/Sub.

## 📊 Опциональный аналитический конвейер

Файл `compose.yaml` включает полностью реализованный

### Компоненты

- **Kafka**: Высокопроизводительная персистентная очередь сообщений для сбора рыночных данных от Aggregator
- **ClickHouse**: Колоночная база данных, оптимизированная для OLAP-запросов, предназначена для хранения исторических рыночных данных
- **Сервис Kafka-ClickHouse**: Простой ETL-сервис для чтения данных из Kafka и выполнения эффективных пакетных вставок в ClickHouse
- **Сервис ClickHouse-Dashboard**: Веб-панель для визуализации данных, хранящихся в ClickHouse

### Варианты использования

- Анализ исторических цен
- Выявление торговых паттернов
- Метрики производительности и отчетность
- Долгосрочное хранение и архивирование данных

## 🚀 Начало работы

### Предварительные требования

- Go 1.21+
- Docker & Docker Compose


### Установка

```bash
# Клонирование репозитория
git clone https://github.com/yourusername/crypto-service.git
cd crypto-service

# Запуск сервисов
docker-compose up -d

# Выполнение миграций
make migrate-up

# Запуск сервисов
make run
```

### Конфигурация

Каждый сервис может быть настроен через переменные окружения или файлы конфигурации. Подробные опции конфигурации смотрите в директориях отдельных сервисов.

## 🔐 Безопасность

- Аутентификация на основе JWT с refresh-токенами
- Безопасные HTTP-only куки для refresh-токенов
- Хеширование паролей с использованием стандартных алгоритмов
- Изоляция баз данных между сервисами
- gRPC для безопасной межсервисной коммуникации

## 📈 Масштабируемость

Микросервисная архитектура обеспечивает:

- Горизонтальное масштабирование отдельных сервисов в зависимости от нагрузки
- Независимое развертывание и обновление
- Выделение ресурсов для конкретных сервисов
- Изоляцию отказов между сервисами

## 🛠️ Стек технологий

- **Язык**: Go
- **Фреймворки**: Gin (HTTP), gRPC
- **Базы данных**: PostgreSQL, Redis
- **Очередь сообщений**: Kafka
- **Аналитика**: ClickHouse
- **Контейнеризация**: Docker



# Детальная документация проекта Crypto-Service

## 📚 Содержание

- [Общая концепция и архитектура](#-общая-концепция-и-архитектура)
- [Детальное описание микросервисов](#-детальное-описание-микросервисов)
- [Базы данных и брокеры сообщений](#-базы-данных-и-брокеры-сообщений)
- [Запуск проекта](#-запуск-проекта)
- [Руководство пользователя](#-руководство-пользователя)
- [Мониторинг и панели управления](#-мониторинг-и-панели-управления)

---

## 🏛️ Общая концепция и архитектура

Crypto-Service — это комплексная система для отслеживания криптовалютных портфелей, построенная на микросервисной архитектуре с использованием Go. Она разделяет бизнес-логику на независимые, слабосвязанные сервисы, которые взаимодействуют друг с другом через синхронные (gRPC, HTTP) и асинхронные (Redis Pub/Sub, Kafka) протоколы.

### Контуры системы

Система состоит из двух основных контуров:

#### 1. Операционный контур (Real-Time)

Отвечает за:
- Аутентификацию пользователей
- Управление портфелями
- Доставку ценовых обновлений в реальном времени через WebSockets

#### 2. Аналитический контур (Data Pipeline)

Отвечает за:
- Сбор исторических рыночных данных
- Передачу данных через Kafka
- Хранение в ClickHouse для последующего анализа

---

## 🔧 Детальное описание микросервисов

### Операционный контур

#### Profile Service (BFF)

**Назначение**: Центральный узел, API-шлюз для всех клиентских взаимодействий

**Технологии**: Go, Gin, GORM, WebSockets

**Взаимодействия**:

**Входящие**:
- HTTP/REST API (порт `:8080`) для клиентов
  - Регистрация и аутентификация
  - Управление портфелем
- WebSocket соединения для обновлений в реальном времени

**Исходящие**:
- gRPC клиент → Authorization Service (аутентификация)
- HTTP клиент → Aggregator Service (управление подписками на тикеры)
- PostgreSQL клиент → `postgres-profile` (хранение профилей)
- Redis клиент (Pub/Sub) → подписка на ценовые каналы

**Ключевые структуры данных**:

```go
// Регистрация/вход
type UserRequest struct {
    Name     string `json:"name"`
    Password string `json:"password"`
}

// Управление монетами
type CoinRequest struct {
    Symbol   string  `json:"symbol"`
    Quantity float64 `json:"quantity"`
}

// Профиль пользователя
type Profile struct {
    ID    uint
    Name  string
    Coins CoinData
}
```

---

#### Authorization Service

**Назначение**: Централизованное управление аутентификацией и авторизацией

**Технологии**: Go, gRPC, GORM, JWT

**Взаимодействия**:

**Входящие**:
- gRPC сервер (порт `:50051`) → запросы от Profile Service

**Исходящие**:
- PostgreSQL клиент → `postgres-auth` (учетные данные)

**Ключевые структуры данных**:

```go
// gRPC запрос
type AuthRequest struct {
    Name     string
    Password string
}

// gRPC ответ
type TokenResponse struct {
    Access  string
    Refresh string
}

// GORM модели
type User struct {
    ID       uint
    Name     string
    Password string // хешированный
}

type RefreshToken struct {
    UserID uint
    Token  string
}
```

---

#### Aggregator Service

**Назначение**: Агрегация и обработка рыночных данных

**Технологии**: Go, Redis, Kafka, gRPC (клиент)

**Взаимодействия**:

**Входящие**:
- HTTP сервер (порт `:8088`) → управляющие команды от Profile Service
- gRPC клиент → получение стримов от Socket Service

**Исходящие**:
- Redis Pub/Sub → публикация `SecondStat` в каналы (например, `btcusdt`)
- Kafka продюсер → отправка `KafkaMsg` в топик `binance.miniticker`

**Ключевые структуры данных**:

```go
// Парсинг сырых данных
type AggTrade struct {
    Symbol string
    Price  string
    Time   int64
}

type MiniTicker struct {
    Symbol string
    Close  string
}

// Ежесекундные данные для Redis
type SecondStat struct {
    Symbol string
    Price  float64
}

// Обогащенные данные для Kafka
type KafkaMsg struct {
    Symbol    string
    Price     float64
    Timestamp int64
}
```

---

#### Socket Service

**Назначение**: Прокси-адаптер к внешним WebSocket-источникам

**Технологии**: Go, Gorilla WebSocket, gRPC

**Взаимодействия**:

**Входящие**:
- gRPC сервер (порт `:50052`) → стримы для Aggregator Service

**Исходящие**:
- WebSocket клиенты → внешние API (Binance)
- Автоматическое управление переподключениями

**Ключевые структуры данных**:

```go
// gRPC-сообщение с сырыми данными
type RawResponse struct {
    Data []byte
}
```

---

### Аналитический контур

#### Kafka-ClickHouse Service

**Назначение**: ETL-сервис для переноса данных из Kafka в ClickHouse

**Технологии**: Go, segmentio/kafka-go, clickhouse-go

**Взаимодействия**:

**Входящие**:
- Kafka консьюмер → чтение из топика `binance.miniticker`

**Исходящие**:
- ClickHouse клиент → пакетные вставки (BatchInsert) в таблицу `market_tickers`

**Ключевые структуры данных**:

```go
// Десериализация из Kafka
type KafkaMsg struct {
    Symbol    string
    Price     float64
    Timestamp int64
}
```

---

## 💾 Базы данных и брокеры сообщений

### PostgreSQL

Две независимые базы данных для изоляции данных:

| База данных | Назначение | Используется |
|-------------|------------|--------------|
| `postgres-auth` | Учетные данные пользователей | Authorization Service |
| `postgres-profile` | Профили и портфели | Profile Service |

### Redis

**Роль**: Быстрая шина сообщений для real-time контура

**Использование**:
- Pub/Sub механизм для трансляции ценовых обновлений
- Каналы именуются по символу монеты (например, `btcusdt`)

### Kafka + Zookeeper

**Роль**: Персистентная, отказоустойчивая очередь для аналитического контура

**Топики**:
- `binance.miniticker` — рыночные данные от Aggregator

### ClickHouse

**Роль**: Колоночная СУБД для хранения и быстрого анализа исторических данных

**Таблицы**:
- `crypto.market_tickers` — исторические рыночные данные

---

## 🚀 Запуск проекта

### Предварительные требования

На вашей машине должны быть установлены:
- Docker
- Docker Compose

### Процедура запуска

1. Откройте терминал в корневой директории проекта

2. Выполните команду для сборки и запуска всех контейнеров:

```bash
docker-compose up --build
```

3. Дождитесь полного развертывания системы. Логи всех сервисов будут выводиться в консоль.

### Доступные эндпоинты

После успешного запуска будут доступны следующие интерфейсы:

| Сервис | URL | Описание |
|--------|-----|----------|
| Profile API | http://localhost:8080 | Основной REST API |
| Redis Commander | http://localhost:8081 | UI для Redis (admin/admin) |
| Tabix | http://localhost:8082 | UI для ClickHouse |
| ClickHouse Dashboard | http://localhost:8083 | Кастомная панель |
| Kafka UI | http://localhost:8084 | Управление Kafka |

---

## 📖 Руководство пользователя

### Шаг 1: Регистрация

Отправьте POST-запрос на `/v1/register`:

**Endpoint**: `http://localhost:8080/v1/register`

**Метод**: `POST`

**Тело запроса**:
```json
{
  "name": "myuser",
  "password": "mypassword123"
}
```

**Ожидаемый ответ**:
```json
{
  "message": "User registered successfully"
}
```

---

### Шаг 2: Вход в систему

Отправьте POST-запрос на `/v1/login`:

**Endpoint**: `http://localhost:8080/v1/login`

**Метод**: `POST`

**Тело запроса**:
```json
{
  "name": "myuser",
  "password": "mypassword123"
}
```

**Ответ**:
```json
{
  "message": "Login successful",
  "🌐 Here is your tokens": {
    "access": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refresh": "..."
  }
}
```

> ⚠️ **Важно**: Сохраните `access_token`. Он понадобится для всех последующих запросов. `refresh_token` автоматически сохраняется в HTTP-only cookie.

---

### Шаг 3: Добавление монет в портфель

Отправьте POST-запрос на `/v2/coin/symbol`:

**Endpoint**: `http://localhost:8080/v2/coin/symbol`

**Метод**: `POST`

**Заголовки**:
```
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**Тело запроса**:
```json
{
  "symbol": "btcusdt",
  "quantity": 1.5
}
```

**Примеры других монет**:
```json
{"symbol": "ethusdt", "quantity": 10.0}
{"symbol": "solusdt", "quantity": 50.0}
```

---

### Шаг 4: Подключение к WebSocket для обновлений в реальном времени

Используйте WebSocket-клиент для подключения к `/v2/user/profile/ws`:

**Endpoint**: `ws://localhost:8080/v2/user/profile/ws`

**Протокол**: WebSocket

**Заголовки**:
```
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

#### Пример с wscat

```bash
wscat -H "Authorization: Bearer YOUR_ACCESS_TOKEN" -c ws://localhost:8080/v2/user/profile/ws
```

#### Пример с JavaScript

```javascript
const ws = new WebSocket('ws://localhost:8080/v2/user/profile/ws', {
  headers: {
    'Authorization': 'Bearer YOUR_ACCESS_TOKEN'
  }
});

ws.onmessage = (event) => {
  const profile = JSON.parse(event.data);
  console.log('Обновление портфеля:', profile);
};
```

#### Формат получаемых сообщений

После подключения вы начнете получать JSON-сообщения с полным состоянием профиля при каждом изменении цены:

```json
{
  "ID": 1,
  "Name": "myuser",
  "Coins": {
    "Quantities": {
      "btcusdt": 1.5,
      "ethusdt": 10.0
    },
    "Prices": {
      "btcusdt": 68123.45,
      "ethusdt": 3456.78
    },
    "Totals": {
      "btcusdt": 102185.175,
      "ethusdt": 34567.80
    }
  }
}
```

---

## 📊 Мониторинг и панели управления

### Kafka UI (`localhost:8084`)

**Возможности**:
- Просмотр топиков (например, `binance.miniticker`)
- Мониторинг сообщений в топиках
- Отслеживание состояния консьюмеров
- Управление конфигурацией Kafka

**Основные разделы**:
- **Topics** — список всех топиков
- **Consumers** — активные группы консьюмеров
- **Brokers** — состояние брокеров Kafka

---

### Redis Commander (`localhost:8081`)

**Учетные данные**: `admin` / `admin`

**Возможности**:
- Просмотр активных Pub/Sub каналов
- Мониторинг данных в Redis
- Анализ используемой памяти
- Управление ключами

**Что искать**:
- Каналы появляются после добавления монет в портфель
- Названия каналов соответствуют символам монет (`btcusdt`, `ethusdt`)

---

### Tabix (`localhost:8082`)

**Назначение**: Веб-интерфейс для работы с ClickHouse

**Возможности**:
- Выполнение SQL-запросов
- Просмотр структуры таблиц
- Экспорт данных

**Примеры запросов**:

```sql
-- Последние 100 записей
SELECT * FROM crypto.market_tickers 
ORDER BY timestamp DESC 
LIMIT 100;

-- Средняя цена за последний час
SELECT 
    symbol,
    avg(price) as avg_price
FROM crypto.market_tickers
WHERE timestamp >= now() - INTERVAL 1 HOUR
GROUP BY symbol;

-- Статистика по символу
SELECT 
    symbol,
    min(price) as min_price,
    max(price) as max_price,
    avg(price) as avg_price,
    count(*) as records
FROM crypto.market_tickers
WHERE symbol = 'btcusdt'
GROUP BY symbol;
```

---

### ClickHouse Dashboard (`localhost:8083`)

**Назначение**: Простая кастомная панель для быстрого просмотра данных

**Возможности**:
- Отображение последних 100 записей из `market_tickers`
- Базовая фильтрация по символу
- Визуализация временных рядов

---

## 🔍 Дополнительная информация

### Порты сервисов

| Сервис | Порт | Протокол |
|--------|------|----------|
| Profile | 8080 | HTTP/WebSocket |
| Aggregator | 8088 | HTTP |
| Authorization | 50051 | gRPC |
| Socket | 50052 | gRPC |
| Redis Commander | 8081 | HTTP |
| Tabix | 8082 | HTTP |
| ClickHouse Dashboard | 8083 | HTTP |
| Kafka UI | 8084 | HTTP |

### Полезные команды Docker

```bash
# Остановка всех сервисов
docker-compose down

# Остановка и удаление volumes
docker-compose down -v

# Просмотр логов конкретного сервиса
docker-compose logs -f profile-service

# Перезапуск конкретного сервиса
docker-compose restart aggregator-service

# Проверка статуса
docker-compose ps
```

### Переменные окружения

Основные переменные окружения можно настроить в файле `.env` или `docker-compose.yaml`:

- `JWT_SECRET` — секретный ключ для JWT
- `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD` — настройки PostgreSQL
- `REDIS_HOST`, `REDIS_PORT` — настройки Redis
- `KAFKA_BROKERS` — адреса брокеров Kafka

---

## 🐛 Устранение неполадок

### WebSocket не подключается

1. Проверьте валидность access token
2. Убедитесь, что добавлена хотя бы одна монета в портфель
3. Проверьте логи Profile Service

### Не приходят обновления цен

1. Убедитесь, что Socket Service подключен к внешнему API
2. Проверьте Redis Pub/Sub каналы в Redis Commander
3. Проверьте логи Aggregator Service

### Ошибки аутентификации

1. Проверьте, что Authorization Service запущен
2. Убедитесь, что PostgreSQL доступна
3. Проверьте корректность JWT_SECRET
